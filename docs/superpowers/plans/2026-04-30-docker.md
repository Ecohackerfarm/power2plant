# Docker Setup (Dev + Prod) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Run the full power2plant stack (Next.js app + PostgreSQL) in Docker for both development (hot reload) and production (optimised image).

**Architecture:** Multi-stage Dockerfile for the app. Extend existing `docker-compose.yml` with an `app` service. Separate `docker-compose.dev.yml` override for dev (volume mounts, NODE_ENV=development). Production compose uses the built image with no mounts.

**Tech Stack:** Docker 24+, docker compose v2, Node.js 20 Alpine, pnpm

---

## File Map

| File | Role |
|------|------|
| `Dockerfile` | Multi-stage build: deps → builder → runner (prod) |
| `docker-compose.yml` | Base: postgres service (already exists) + app service (prod) |
| `docker-compose.dev.yml` | Dev override: app with volume mounts + hot reload |
| `.dockerignore` | Exclude node_modules, .worktrees, .env, build artefacts |
| `README.md` | Add Docker usage section |

---

## Task 1: `.dockerignore`

**Files:**
- Create: `.dockerignore`

- [ ] **Step 1: Create `.dockerignore`**

```
node_modules
.next
.worktrees
.env
*.local
prisma/migrations/*.sql (keep — needed for migrate deploy)
```

Actually keep migrations. Final `.dockerignore`:

```
node_modules
.next
.worktrees
.env
.env.local
*.log
npm-debug.log*
.DS_Store
```

- [ ] **Step 2: Commit**

```bash
git add .dockerignore
git commit -m "chore: add .dockerignore"
```

---

## Task 2: Multi-Stage `Dockerfile`

**Files:**
- Create: `Dockerfile`

- [ ] **Step 1: Create `Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1

# ---- deps ----
FROM node:20-alpine AS deps
RUN corepack enable && corepack prepare pnpm@latest --activate
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

# ---- builder ----
FROM node:20-alpine AS builder
RUN corepack enable && corepack prepare pnpm@latest --activate
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN pnpm exec prisma generate
RUN NODE_ENV=production pnpm build

# ---- runner ----
FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
RUN addgroup --system --gid 1001 nodejs && \
    adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
COPY --from=builder /app/prisma ./prisma
COPY --from=builder /app/node_modules/.prisma ./node_modules/.prisma
COPY --from=builder /app/node_modules/@prisma ./node_modules/@prisma

USER nextjs
EXPOSE 3000
ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

# Run migrations then start
CMD ["sh", "-c", "node_modules/.bin/prisma migrate deploy && node server.js"]
```

**Note:** `output: 'standalone'` must be enabled in `next.config.ts` for the runner stage to work:

```typescript
// next.config.ts
const nextConfig: NextConfig = {
  output: 'standalone',
  // ... existing config
}
```

- [ ] **Step 2: Enable standalone output in `next.config.ts`**

Add `output: 'standalone'` to the NextConfig object in `next.config.ts`.

- [ ] **Step 3: Verify build locally**

```bash
docker build -t power2plant:test .
```

Expected: build completes, all 3 stages succeed.

- [ ] **Step 4: Commit**

```bash
git add Dockerfile next.config.ts
git commit -m "feat: add multi-stage Dockerfile with standalone Next.js output"
```

---

## Task 3: Update `docker-compose.yml` (Production)

**Files:**
- Modify: `docker-compose.yml`

- [ ] **Step 1: Add app service to `docker-compose.yml`**

Replace `docker-compose.yml` with:

```yaml
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: power2plant
      POSTGRES_PASSWORD: power2plant
      POSTGRES_DB: power2plant
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U power2plant"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://power2plant:power2plant@db:5432/power2plant
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
```

`BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `NEXT_PUBLIC_APP_URL` are read from the host `.env` file (or environment). The DB URL is hardcoded to the internal service name.

- [ ] **Step 2: Test production compose**

```bash
docker compose up --build
```

App should be accessible at http://localhost:3000.

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "feat: add app service to docker-compose.yml (production)"
```

---

## Task 4: `docker-compose.dev.yml` (Development)

**Files:**
- Create: `docker-compose.dev.yml`

Development uses volume mounts so Next.js hot reload works without rebuilding the image. Node modules are NOT mounted (image's node_modules is used — avoids platform mismatch).

- [ ] **Step 1: Create `docker-compose.dev.yml`**

```yaml
services:
  app:
    build:
      context: .
      target: deps        # only install deps stage, no build
    command: sh -c "pnpm exec prisma generate && pnpm exec prisma migrate dev --name auto && pnpm dev"
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://power2plant:power2plant@db:5432/power2plant
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:-dev-secret-change-in-production}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
    volumes:
      - .:/app
      - /app/node_modules    # keep container's node_modules (platform-specific binaries)
      - /app/.next           # keep container's .next (built for linux)
    depends_on:
      db:
        condition: service_healthy
```

Usage:
```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up
```

- [ ] **Step 2: Test dev compose**

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up
```

Expected: app starts with hot reload, edits to `src/` reflect immediately.

- [ ] **Step 3: Commit**

```bash
git add docker-compose.dev.yml
git commit -m "feat: add docker-compose.dev.yml with hot-reload volume mounts"
```

---

## Task 5: Update README.md

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add Docker section to README**

Add after the existing "Getting Started" section:

```markdown
## Docker

### Development (hot reload)

```bash
cp .env.example .env
docker compose -f docker-compose.yml -f docker-compose.dev.yml up
```

Edit files in `src/` — Next.js hot reloads automatically.

### Production

```bash
# Set required env vars
export BETTER_AUTH_SECRET="your-secret-here"
export BETTER_AUTH_URL="https://your-domain.com"
export NEXT_PUBLIC_APP_URL="https://your-domain.com"

docker compose up --build
```

Migrations run automatically on container start.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add Docker usage section to README"
```

---

## Done

Full Docker setup:
- `Dockerfile`: multi-stage, Alpine, standalone Next.js output, runs `prisma migrate deploy` on start
- `docker-compose.yml`: postgres + app (production)
- `docker-compose.dev.yml`: override for dev with volume mounts + hot reload
- `.dockerignore`: excludes build artefacts and secrets

**Known issue to verify:** `prisma migrate dev` in the dev container requires an interactive TTY. If it hangs, replace with `prisma migrate deploy` in the dev command (dev and prod use the same migration runner).
